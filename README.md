# EmployeeManagementBackendApplication
EmployeeManagementBackendApplication is a backend project built using Java and Spring Boot to manage employee details. It supports basic CRUD operations through REST APIs and follows a clean layered architecture with Controller, Service, and Repository. Spring Data JPA and MySQL are used for database operations
